{
  "name": "rose",
  "version": "1.0.0",
  "private": true,
  "description": "RoSE — Reasoning with Orchestrated Streaming Experiences (local Ollama demo)",
  "scripts": {
    "dev": "./start.sh",
    "start": "./start.sh",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend run dev",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "prepare-data": "npm --prefix backend run prepare-data"
  }
}
